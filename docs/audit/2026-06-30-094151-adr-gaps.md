# ADR Gaps Audit: 2026-06-30-094151

## Summary

| Metric                   | Value                                                                              |
| ------------------------ | ---------------------------------------------------------------------------------- |
| Scope                    | repo (/Users/thkt/GitHub/cli/gates)                                                |
| Source files scanned     | 18                                                                                 |
| Documents scanned        | 3 (README.md, .claude/OUTCOME.md, Cargo.toml [lints])                              |
| Decision candidates      | 6 net-new (post 2026-06-23 census + ADR-0001..0004 authoring)                      |
| ADR-covered (excluded)   | 5 (ADR-0001..0004 local + depgraph import-resolution → ADR-0002 "Bad" consequence) |
| Net new candidates       | 4 (challenged)                                                                     |
| ADR promotion candidates | 2 (post-challenge `keep`)                                                          |

This is an incremental census: the 2026-06-23 run (pre-ADR) nominated 4 candidates that became ADR-0001..0004, plus 6 downgrade/drop/bug items. This pass mines only what changed since — the decisions introduced by PRs #90–#100 (advisory/`Warned` tier, `GATES_` prefix, `$HOME` fence, sister-hook consistency fixes) — and re-validates that the 4 ADRs now cover their predecessors. Mining used three `reviewer-rust` subagents over the changed files (tools.rs, traverse/project/resolve, runner/main/config/hook_exit); candidates were challenged by `critic-design`. Commit-message mining (3b) surfaced the four feature themes above; rationale otherwise lives in code-doc, README, and OUTCOME.md, not commit bodies. `unsafe_code = "forbid"` (Cargo.toml) is statement-of-fact config, not ADR-worthy.

## Source File Decisions

### src/runner.rs (309 lines)

| #   | Line     | Decision                                                                                    | Documented? | Incomplete-contract? | Impact | Reversibility |
| --- | -------- | ------------------------------------------------------------------------------------------- | ----------- | -------------------- | ------ | ------------- |
| 1   | 13-19,60 | `GateOutcome::Warned` — 4th outcome (advisory: stderr-only, never a block decision, exit 0) | Partial     | Yes                  | H      | medium        |

### src/tools.rs (1921 lines)

| #   | Line                    | Decision                                                                                                         | Documented? | Incomplete-contract? | Impact | Reversibility |
| --- | ----------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------- | -------------------- | ------ | ------------- |
| 1   | 441,685,831             | Three `Warned` producers (litmus Warning tier / jscpd-no-block / TS2307 downgrade), no central tier rule         | Partial     | Yes                  | H      | medium        |
| 2   | 572 vs 684              | Opposite default posture: `run_clone` always Failed (block) vs `jscpd_outcome` Warned (advisory)                 | No          | Yes                  | M      | medium        |
| 3   | 132-148                 | `GATES_`-prefixed env overrides are the override interface; no bare fallback (#95)                               | Yes         | No                   | H      | low           |
| 4   | 152-157                 | Package-manager precedence pnpm > bun > yarn > npm on coexisting lockfiles                                       | No          | Yes                  | L      | high          |
| 5   | 345-360,405-422,778-834 | litmus ANALYZER_STACK_SIZE mirror / tier exhaustive-match / TS2307 whole-run downgrade (all carry forward rules) | Yes         | No                   | M–H    | medium        |

### src/traverse.rs (92 lines) / src/project.rs (94 lines)

| #   | Line                              | Decision                                                                                                  | Documented? | Incomplete-contract? | Impact | Reversibility |
| --- | --------------------------------- | --------------------------------------------------------------------------------------------------------- | ----------- | -------------------- | ------ | ------------- |
| 1   | traverse.rs:6-26                  | Ancestor walk fences at `$HOME` (framed as formatter-alignment, not a containment invariant)              | Partial     | Yes                  | H      | medium        |
| 2   | traverse.rs:20 / project.rs:24-29 | Project root = nearest `.git` ancestor; silent fallback-to-`start` when none; nested package → outer repo | No          | Yes                  | M      | high          |
| 3   | traverse.rs:4                     | `MAX_TRAVERSAL_DEPTH = 20` bare cap, rationale unstated                                                   | No          | No                   | L      | high          |

### src/config.rs (311 lines) / src/hook_exit.rs (90 lines)

| #   | Line                      | Decision                                                                                                            | Documented?           | Incomplete-contract? | Impact | Reversibility |
| --- | ------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------- | -------------------- | ------ | ------------- |
| 1   | config.rs:68-75 + 5 sites | Sister-hook mirror-vs-diverge meta-invariant (config fail-open / exit / separators / color / traverse)              | Partial               | Yes                  | M      | low           |
| 2   | config.rs:68-75           | config-load fail-OPEN (broken/missing → all enabled), distinct axis from ADR-0001/0004 (rationale in-comment)       | Yes                   | No                   | M      | medium        |
| 3   | hook_exit.rs:25,31        | STALE prose "every gate today is blocking" — false since 3 `Warned` producers (Advisory(1) exit code still dormant) | No (contradicts code) | Yes                  | M      | high          |

### src/resolve.rs (62 lines)

| #   | Line  | Decision                                                                                     | Documented? | Incomplete-contract? | Impact | Reversibility |
| --- | ----- | -------------------------------------------------------------------------------------------- | ----------- | -------------------- | ------ | ------------- |
| 1   | 11-13 | `resolve_bin` rejects names with `/` or `..` (path-injection guard, not labeled as boundary) | No          | Yes                  | M      | medium        |
| 2   | 14-19 | Nearest-ancestor `node_modules/.bin` + exec-bit, else bare-name → `$PATH` (Unix-only)        | No          | No                   | M      | medium        |

No net-new decisions in src/clone.rs, src/snapshot.rs, src/audit.rs, src/circular.rs, src/reporter.rs, src/depgraph.rs, src/coupling.rs, src/sanitize.rs, src/color.rs, src/test_utils.rs, src/main.rs (depgraph import-resolution scope is already recorded as ADR-0002's "Bad" consequence; main.rs dispatch asymmetry cites FR-006/#17 and is self-documented).

## Prose Document Decisions

### README.md

| #   | Line    | Decision Verb | Decision                                                              | ADR Coverage         | Impact | Reversibility |
| --- | ------- | ------------- | --------------------------------------------------------------------- | -------------------- | ------ | ------------- |
| 1   | 280     | keeps/never   | `GATES_` prefix keeps overrides from colliding with bare CI vars      | None (→ candidate D) | H      | low           |
| 2   | 181,189 | follows       | Exit codes follow ADR-0066 Group 3; advisory/blocking → stdout+exit 0 | ADR-0066 (external)  | H      | low           |

### .claude/OUTCOME.md

| #   | Line  | Decision Verb   | Decision                                                                          | ADR Coverage                | Impact | Reversibility |
| --- | ----- | --------------- | --------------------------------------------------------------------------------- | --------------------------- | ------ | ------------- |
| 1   | 27-30 | 役割分担/不採用 | guardrails=pre lightweight / gates=post full-audit role split; diff-only rejected | None (→ candidate C ground) | M      | low           |
| 2   | 45    | 従う            | exit-code type, advisory=1/blocking=2 reserved (switchable-later #18)             | ADR-0066 (external)         | H      | low           |

### Cargo.toml [lints]

`unsafe_code = "forbid"` — statement-of-fact config, source-of-truth in the manifest itself; no ADR (would create drift risk). No other decisions found.

## ADR Promotion Candidates (post-challenge)

| #   | Candidate                                                                                                                                                                                                                                                                                                                                        | Initial | Challenge | Final                    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | --------- | ------------------------ |
| A   | runner.rs:13-19 / tools.rs:441,685,831 - Advisory outcome tier (`GateOutcome::Warned`): ran-and-would-fail-but-advisory → Warn (stderr, exit 0), can't-run → Skip (ADR-0001), confident-fail → Fail. Author-facing Skip/Warn/Fail decision rule; type+test enforce behavior but not the choice                                                   | promote | keep      | ADR (cross-ref ADR-0001) |
| C   | config.rs:68-75 + hook_exit.rs:2 + reporter.rs:4 + traverse.rs:10 + color.rs:10 - Sister-hook **burden-of-proof** rule: mirror is the default for shared protocol/presentation surfaces; divergence requires documented role-based justification (gates = post-edit-audit half, OUTCOME.md:27-30). Cross-repo meta-rule with no single-file home | promote | keep      | ADR                      |
| B   | traverse.rs:6-26 / project.rs:24-29 - Filesystem traversal `$HOME` fence + `.git`-as-root. Threat model thin (start = hook cwd, depth-bounded, tested); only the missing forward rule is a gap                                                                                                                                                   | promote | downgrade | inline-comment           |
| D   | tools.rs:132-148 / README.md:280 - `GATES_` env-override namespace. Proposed broad rule "never read bare" is already false (`HOME`/`XDG_DATA_HOME`/`NO_COLOR` read bare); correctly-scoped "gate _command_ overrides must be GATES\_-namespaced" is ~90% in comment+README                                                                       | promote | downgrade | inline-comment           |

keep 2 / downgrade 2 / drop 0

## Bug-fix follow-ups (not ADRs)

- **hook_exit.rs:25,31** — stale comment "every gate today is blocking" / "Advisory (1) has no producer yet"; false since `GateOutcome::Warned` gained 3 producers. The exit-code `Advisory(1)` is still genuinely dormant (Warned → stderr → exit 0), so fix the prose to distinguish "no advisory gate exists" (false) from "no advisory outcome reaches the exit code" (true). Surfaces as the symptom of candidate A's unrecorded decision.
- **project.rs:24-29** — nested-package monorepo resolves to the outer `.git` root, not the inner package owning tsconfig (outer root → `has_tsconfig` false → type gate skips at the package that owns it). Bug-vs-invariant unresolved; route to a bug investigation rather than documenting the current behavior as intentional.

# ADR Drift Scan: 2026-06-30-092803

## Summary

| Metric            | Value |
| ----------------- | ----- |
| ADRs scanned      | 4     |
| Drift findings    | 4     |
| H priority        | 0     |
| M priority        | 0     |
| L priority        | 4     |
| Unverifiable ADRs | 0     |

All four ADRs are `accepted`; none superseded. Every binding contract symbol, enum variant, struct field, and Confirmation test named by the ADRs is present in code and semantically intact. The four findings are all L-priority and confined to stale `file:line` references in ADR-0001 and ADR-0002 prose: line numbers drifted behind refactors merged after the ADRs were authored (the same class of drift the 2026-06-24 scan flagged for ADR-0001, re-introduced by PRs #95–#100). No contract symbol moved, was renamed, or was removed; semantics are unchanged. ADR-0003 and ADR-0004 show no drift.

## Per-ADR Findings

ADRs 0003, 0004: no drift. AuditEntry `{ts, project, decision, failed}` (audit.rs:16-21) and the append path (audit.rs:37-50) match ADR-0003 exactly; `is_enabled` (config.rs:61) plus tests `missing_file_enables_all_gates` / `partial_gates_section` (config.rs) match ADR-0004.

### ADR 0001: Adopt fail-open invariant for all gates

Status: Accepted
Result: 3 findings (line-reference drift only; policy, sink, and all three Confirmation tests intact)

Verified intact: `GateOutcome::Skipped` at runner.rs:16 (line ref exact); `join_or_skip` canonical-policy doc-comment carries the `ADR-0001` tag at runner.rs:204; tests `invalid_json_enables_all_gates` (config.rs), `query_skips_corrupt_lines` (audit.rs), `join_into_maps_panic_to_skipped_per_fallback_name` (main.rs:524) all present. Note: `GateOutcome` gained a `Warned` advisory variant (runner.rs:17-19) since the ADR — orthogonal to fail-open (advisory ≠ error path), so not a contract drift.

| File:Line     | Description                                                                                                    | Direction  | Priority |
| ------------- | -------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| runner.rs:204 | ADR cites `join_or_skip` at runner.rs:204; line 204 is now the policy doc-comment, `fn` is at 210              | adr-update | L        |
| main.rs:118   | ADR cites `join_into` at main.rs:118 (×2: Context + Confirmation); line 118 is the doc-comment, `fn` is at 119 | adr-update | L        |

### ADR 0002: Embed oxc-based analysis instead of shelling out

Status: Accepted
Result: 2 findings (line-reference drift only; embedding decision and detection semantics intact)

Verified intact: oxc-only parser deps (depgraph.rs:1-4 imports `oxc_allocator/ast/parser/span`); single shared parse (depgraph.rs:11-13 doc); three-color back-edge detection still present — `Color` enum `White/Gray/Black` (circular.rs:28-32), now an explicit-stack **iterative** DFS (circular.rs:47 doc, `dfs` at 54), consistent with ADR-0001's stackification note and still "back-edge cycles, not minimal SCCs"; module resolution honors `.`-relative + `.ts/.tsx/index` only, no tsconfig aliases (`resolve_import` depgraph.rs:131, `index.{ext}` at 150).

| File:Line       | Description                                                                                                                 | Direction  | Priority |
| --------------- | --------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| circular.rs:27  | ADR cites three-color DFS at circular.rs:27-99; algorithm region is now ~28-103 (enum 28-32, `dfs` 54, `record_cycle` 94)   | adr-update | L        |
| depgraph.rs:131 | ADR cites module resolution at depgraph.rs:131-157; `resolve_import` starts at 131, body ends ~157 (range slightly shifted) | adr-update | L        |

## External ADR Dependencies

| File:Line                                 | External ADR ref     | Recommended action                                                                                          |
| ----------------------------------------- | -------------------- | ----------------------------------------------------------------------------------------------------------- |
| README.md:181, hook_exit.rs:1, main.rs:29 | ADR-0066 (not local) | Keep as external dependency on shared dotclaude config; promote locally only if hook exit-code policy forks |
| hook_exit.rs:26, main.rs:37               | ADR-0060 (not local) | Keep as external dependency (gates show I/O code); promote locally only if it diverges from upstream        |

Note: the `_lib/external-adr-refs.py` helper named by the skill is absent; the external cross-check was run manually via `ugrep -P 'ADR-\d{4}'`. ADR-0066 and ADR-0060 resolve to sibling-repo (dotclaude / guardrails-shared) ADRs with no `NNNN-*.md` local match — unchanged from the 2026-06-24 scan.

## Follow-up Issue Candidates

- [ ] ADR 0001 drift at runner.rs:204 / main.rs:118: update `join_or_skip` / `join_into` line refs to 210 / 119
- [ ] ADR 0002 drift at circular.rs:27 / depgraph.rs:131: update DFS and module-resolution line ranges to current
